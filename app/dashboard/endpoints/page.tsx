'use client';

import { ComingSoon } from '@/components/ComingSoon';

export default function EndpointsPage() {
    return (
        <ComingSoon
            title="Endpoint Management"
            description="Advanced endpoint monitoring, remote shell access, and fleet management capabilities are currently being built. Stay tuned for the next release."
        />
    );
}
